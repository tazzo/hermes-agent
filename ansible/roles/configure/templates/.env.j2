# API keys are entered by the user via the dashboard (hermes config set → saves here).
# This file is only created if absent (force: no) — user state wins on rebuilds.
OPENAI_API_KEY=
TELEGRAM_BOT_TOKEN=
# Groq API key (voice/LLM provider for Discord bot) — provisioned via gopass env
# (GROQ_API_KEY exported at playbook run), never stored in git. See configure tasks.
GROQ_API_KEY={{ lookup('env', 'GROQ_API_KEY') | default('', true) }}
